// Criado as constantes para ações do contador.
const INCREMENTAR = "contador/INCREMENTAR";
const REDUZIR = "contador/REDUZIR";

const incrementar = () => ({ type: INCREMENTAR }); // Criado uma constante chamada incrementar que retorna um objeto com a propriedade type, ou seja, a ação que será executada.
